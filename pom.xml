<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.sonatype.oss</groupId>
    <artifactId>oss-parent</artifactId>
    <version>7</version>
  </parent>

  <groupId>com.samskivert.scaled</groupId>
  <artifactId>scaled-project</artifactId>
  <packaging>pom</packaging>
  <version>1.0-SNAPSHOT</version>

  <name>Scaled Meta-Project</name>
  <description>A scalable editor.</description>
  <url>http://github.com/scaled/scaled/</url>

  <scm>
    <connection>scm:git:https://github.com/scaled/scaled.git</connection>
    <url>https://github.com/scaled/scaled/</url>
  </scm>

  <licenses>
    <license>
      <name>The (New) BSD License</name>
      <url>http://www.opensource.org/licenses/bsd-license.php</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>samskivert</id>
      <name>Michael Bayne</name>
      <email>mdb@samskivert.com</email>
    </developer>
  </developers>

  <properties>
    <scala.version>2.11.0</scala.version>
    <scala.major.version>2.11</scala.major.version>
  </properties>

  <modules>
    <module>api</module>
    <module>pacman</module>
    <module>editor</module>
    <module>modes</module>
    <module>libs</module>
    <module>services</module>
  </modules>

  <!-- common depends for all modules -->
  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.11</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <profiles>
    <profile>
      <!-- enable this to test the editor interactively -->
      <id>devel</id>
      <modules>
        <module>devel</module>
      </modules>
    </profile>
  </profiles>

  <build>
    <!-- common plugin config for all modules -->
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.1</version>
          <configuration>
            <source>1.8</source>
            <target>1.8</target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.scala-tools</groupId>
          <artifactId>maven-scala-plugin</artifactId>
          <version>2.15.2</version>
          <executions>
            <execution>
              <id>compile</id>
              <goals><goal>compile</goal></goals>
              <phase>compile</phase>
            </execution>
            <execution>
              <id>test-compile</id>
              <goals><goal>testCompile</goal></goals>
              <phase>test-compile</phase>
            </execution>
            <execution>
              <phase>process-resources</phase>
              <goals><goal>compile</goal></goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
